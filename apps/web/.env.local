# NEXT_PUBLIC_API_URL não é mais necessário — o proxy em next.config.js roteia /api para o NestJS.
# Defina API_INTERNAL_URL se o NestJS rodar em outra porta ou host (ex: containers).
# API_INTERNAL_URL=http://localhost:3001/api
